package router

const (
	GoMarsService = "/GoMarsService"
)

const (
	Car =  GoMarsService + "/Car"
	LaunchNow = "/LaunchNow"
	SendCommand = "/SendCommand"
	GetCoverage = "/GetCoverage"
	GetCurrentPosition = "/GetCurrentPosition"
)